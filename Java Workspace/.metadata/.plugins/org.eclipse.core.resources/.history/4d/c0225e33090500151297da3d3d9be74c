package velinov.java.lut;


/**
 * @author vladimirvelinov
 * @since May 27, 2015
 */
public class SpineLookUpTable {
  
  private float[] negAngles;
  private long[]  posAnglesCount;
  
  private int     tableSize;
  private float   maxAngle;
  
  /**
   * @param _minAngle
   * @param _maxAngle
   * @param _offset
   */
  public SpineLookUpTable(int _minAngle, int _maxAngle, int _offset) {

    this.tableSize   = ((_maxAngle - _minAngle) / _offset) + 1;
    this.negAngles      = new float[this.tableSize];
    this.posAnglesCount = new long[this.tableSize];
    this.maxAngle    = _maxAngle;
    
    for (int i = 0; i < this.tableSize; i ++) {
      this.negAngles[i] = _minAngle + (i * _offset);
    }
    
  }
  
  /**
   * @param _angle
   */
  public void setAngle(float _angle) {
    int index;
    float f;
    
    f = (_angle * ((this.tableSize - 1)/ this.maxAngle));
    
    index = (int) Math.floor(f);
    
   // this.anglesCount[index] ++;
    
    System.out.println(_angle + " " + index);
  }
}
